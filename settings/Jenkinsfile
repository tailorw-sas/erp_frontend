pipeline {
    
    agent any

	environment {
		GIT_PACKAGE_TOKEN = credentials("GithubPackageToken")
	}
	
	triggers {
        githubPush()
    }
    
    stages {
        stage ('Init') {
            steps {
                echo 'Initializing...'
            }
        }

	stage ('Create docker image'){
		steps {
			dir('settings'){
				sh 'docker build -t tailorw/fin-settings:dev --build-arg PACKAGE_TOKEN=$GIT_PACKAGE_TOKEN .'
				withDockerRegistry(credentialsId: 'Tailorw-DockerCredentials', url: "") {
                			sh 'docker push tailorw/fin-settings:dev'
                		}
			}
		}
	}

	stage ('Publish to Kubernetes') {
            steps {
                withKubeConfig(caCertificate: '', clusterName: '', contextName: '', credentialsId: 'KubernetesCredential', namespace: 'finamer', restrictKubeConfigAccess: false, serverUrl: 'https://172.20.41.100:5443') {
                    sh 'kubectl apply -f kubernetes-config/deployments/settings.yaml'
                }
            }
        }
    }
}
